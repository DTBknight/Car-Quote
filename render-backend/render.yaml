services:
  - type: web
    name: car-quote-backend
    env: python
    rootDir: render-backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn render:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18 